# 現行サマリ

> **更新**: 2026-07-14  
> **詳細・履歴の正本**: [ポリゴンアプリ再設計_e54196e6.plan.md](ポリゴンアプリ再設計_e54196e6.plan.md)  
> **使い方**: 新チャットでは `@現行サマリ.md` を付けて着手。875行超の計画書全文は通常不要。

---

## 進捗

| 項目 | 内容 |
|------|------|
| **完了** | Phase A〜E+（溶接・座標継ぎ目・磁石・Undo・頂点編集・detach/weld・Undo上限・weld figure-8対策） |
| **次** | **Phase G-spike** |
| **以降** | Hα → Hβ → F → G → Hγ → Hδ → R |

**v1 の芯**: 下絵 → ズーム → なぞり → 三角ローポリ（G）→ 保存 → PNG

---

## 今やること: Phase G-spike

**目的**: v1 の目玉（G）の de-risk。本番実装前に **0.5〜2日** で検証。Hα より前に実施。

| 内容 | 備考 |
|------|------|
| ドロネー/制約付き三角分割のパッケージ候補を1つ動かす | 自前 vs 移植 vs pub.dev |
| 代表閉曲線1つを三角分割し `Artwork` に投入できるか確認 | 紙上設計でも可 |
| 自己交差・退化入力でどう壊れるか把握 | G 本番 #8 の材料に |
| `compute()`（Isolate）で分離実行できるか検証（#17） | ANR 対策の前提確認 |
| `ArtworkDocument` v1 スキーマを紙上で確定（U1 / #9） | 幾何フィールド・`UnderlayLayout` の JSON 位置・`canvasSize` 非永続 |
| 仮 `maxEdge` で「max 超え三角が残らないか」確認（#20） | **物理サイズは未確定**、仮値でよい |

### 完了条件

- [ ] spike 結果を検討メモに記録
- [ ] Go/No-Go 判断（パッケージ・Isolate・**Tier B/C**・U1 スキーマ）
- [ ] max 超え三角の有無（仮 maxEdge 可）
- [ ] 本番 merge 不要（spike ブランチ or 捨てコード可）

### やらないこと

- 本番コードへの統合
- `maxEdge`/`minEdge` の **確定値**を決めること（G 本番着手前に別途相談）
- 実機確認（紙上・spike コードのみで可）

### 参照

計画書「Phase G-spike」「コード品質・修正前提」着手前チェックリスト **G-spike（Hα より前）**

---

## 直近の重要決定（参照用）

- **G 三角サイズ（#20）**: `maxEdge` + `minEdge = maxEdge × 0.4`。**world 値は G 着手前に相談で確定**（今は決めない）
- **G-spike**: Go → Tier B（ドロネー + max 再分割）／ No-Go → Tier C（格子 + 辺分割）
- **U1**: `ArtworkDocument` v1 スキーマ → G-spike 完了時に紙上確定
- **統合 smoke（U2）**: Hγ 完了時・R QA 前に手動フル実行
- **Undo 上限**: `kUndoStackLimit = 100`（E+ で実装済み）

---

## 更新ルール

1. **Phase 完了コミット後**、本ファイルの「今やること」を次 Phase に差し替える
2. 計画書 frontmatter の `overview` 1行も「次 Phase 名」だけ合わせて更新してよい
3. 仕様変更・経緯の詳細は計画書の検討メモへ（本ファイルは短く保つ）

**G-spike 完了後の次**: 本ファイルを **Phase Hα** 用に更新する
